<?php

/*
|--------------------------------------------------------------------------
| タイムスタンプの補足除去設定
|--------------------------------------------------------------------------
|
| 「曲名 / アーティスト (エコーかけ忘れ)」のように、曲名とは無関係な補足が
| 付いたタイムスタンプを正規化前に掃除するための辞書。
|
| App\Helpers\SupplementStripper が参照する。
| 影響の確認は `php artisan ts-items:analyze-supplements` で行える。
|
*/

return [
    /*
     * 補足キーワード
     *
     * 括弧の中身、または区切り（全角スペース等）以降にこれらが含まれる場合、
     * その塊を「曲名ではない補足」とみなして除去する。
     *
     * 判定は TextNormalizer::normalize() を通した上での部分一致なので、
     * 大文字小文字・全角半角の違いは吸収される。
     *
     * 注意: 短い英字キーワード（2文字程度）は無関係な語の一部に一致しやすいので
     * 追加は慎重に。曲名そのものになりうる語（full, short 等）も避けること。
     */
    'keywords' => [
        // 収録・音声まわりの注記
        'エコー',
        'かけ忘れ',
        'かけ間違い',
        '音量',
        'ボリューム',
        'マイク',
        '音ズレ',
        'ノイズ',
        'ハウリング',
        '無音',
        'bgm',

        // 進行に関する注記
        'アンコール',
        'encore',
        'リクエスト',
        'リハ',
        '雑談',
        'ネタバレ',

        // 歌い方・尺に関する注記
        'ワンコーラス',
        '1番のみ',
        '2番のみ',
        'サビのみ',
        '途中から',
        '途中まで',
        '歌詞間違い',
        '歌詞ミス',
    ],

    /*
     * 装飾記号
     *
     * テキストの先頭・末尾に付いているだけの飾り。無条件で除去する。
     * 曲名の内部には手を付けないので、記号を含む曲名は壊れない。
     */
    'symbols' => [
        // 音符・音楽系
        '♪', '♫', '♬', '♩', '🎵', '🎶', '🎼', '🎤', '🎙', '🎧',

        // 星・キラキラ系
        '★', '☆', '✦', '✧', '✩', '✰', '⭐', '✨', '🌟', '💫',

        // 図形・記号系
        '※', '▶', '▷', '►', '▸', '◆', '◇', '■', '□', '●', '○', '◎',

        // ハート系
        '♡', '♥', '❤', '💖', '💕', '💗',

        // 矢印・箇条書き系
        '→', '⇒', '‣', '・', '･',
    ],
];
