<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Song Similarity Threshold
    |--------------------------------------------------------------------------
    |
    | この値は楽曲の類似度判定に使用されるしきい値です。
    | 0.0 ～ 1.0 の範囲で設定します。
    | 例: 0.75 = 75%以上の類似度で「類似曲」とみなす
    |
    */
    'similarity_threshold' => env('SONG_SIMILARITY_THRESHOLD', 0.75),

    /*
    |--------------------------------------------------------------------------
    | Fuzzy Search Threshold
    |--------------------------------------------------------------------------
    |
    | あいまい検索で使用するデフォルトのしきい値です。
    |
    */
    'fuzzy_search_threshold' => env('SONG_FUZZY_SEARCH_THRESHOLD', 0.7),

    /*
    |--------------------------------------------------------------------------
    | Auto Link Settings
    |--------------------------------------------------------------------------
    |
    | 自動紐付け機能の設定です。
    |
    */
    'auto_link' => [
        // 1リクエストあたりの遅延時間（ミリ秒）
        'delay_ms' => env('AUTO_LINK_DELAY_MS', 500),

        // レート制限エラー時の待機時間（秒）
        'rate_limit_wait_seconds' => env('AUTO_LINK_RATE_LIMIT_WAIT_SECONDS', 60),

        // 自動紐付け時の類似度チェックしきい値（高めに設定）
        'similarity_threshold' => env('AUTO_LINK_SIMILARITY_THRESHOLD', 0.9),
    ],
];
