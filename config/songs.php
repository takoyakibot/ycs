<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Song Similarity Threshold
    |--------------------------------------------------------------------------
    |
    | この値は楽曲の類似度判定に使用されるしきい値です。
    | 0.0 ～ 1.0 の範囲で設定します。
    | 例: 0.75 = 75%以上の類似度で「類似曲」とみなす
    |
    */
    'similarity_threshold' => env('SONG_SIMILARITY_THRESHOLD', 0.75),

    /*
    |--------------------------------------------------------------------------
    | Fuzzy Search Threshold
    |--------------------------------------------------------------------------
    |
    | あいまい検索で使用するデフォルトのしきい値です。
    |
    */
    'fuzzy_search_threshold' => env('SONG_FUZZY_SEARCH_THRESHOLD', 0.7),

    /*
    |--------------------------------------------------------------------------
    | Auto Link Settings
    |--------------------------------------------------------------------------
    |
    | 自動紐付け機能の設定です。
    |
    */
    'auto_link' => [],

    /*
    |--------------------------------------------------------------------------
    | Song Matching Settings
    |--------------------------------------------------------------------------
    |
    | タイムスタンプのテキストと楽曲マスタの照合設定です。
    | 照合は「マスタのタイトルがテキストに含まれるか」で行い、
    | 一致の確実さを信頼度（0.0 ～ 1.0）として算出します。
    |
    | auto_link_threshold: この値以上の信頼度で自動紐付けします。
    | candidate_threshold: この値以上の信頼度を画面上の候補として提示します。
    |
    | 信頼度の水準は SongMatchingService の CONFIDENCE_* 定数を参照してください。
    | 閾値を下げると紐付け件数は増えますが誤爆も増えます。
    |
    | 自動紐付けは既定で無効にしている（1.0）。信頼度の最大値は
    | SongMatchingService::CONFIDENCE_EXACT と
    | MappingDictionaryService::CONFIDENCE_KEY_MATCH の 0.95 なので、
    | 1.0 では findBestMatch() が必ず null を返し、自動での書き込みは発生しない。
    | 候補の「表示」は candidate_threshold が独立に制御するため影響を受けない。
    |
    | 無効にしている理由は、現在の信頼度の付け方では誤った紐付けを自動で
    | 書き込んでしまうため。実測された誤爆（Issue #672）:
    |   愛言葉Ⅲ / DECO*27 → 愛言葉 / DECO*27  被覆率0.30 信頼度0.90
    |   Full Moon         → Moon              信頼度0.95
    |   カバーガール       → ガール             信頼度0.95
    |   愛言葉Ⅲ           → 愛言葉Ⅱ（辞書経由） 信頼度0.85
    | 被覆率は正当な一致（0.1429）より誤爆（0.30）のほうが高くなるため、
    | 閾値の調整では分離できない。信頼度の設計を作り直してから有効化すること。
    |
    */
    'matching' => [
        'auto_link_threshold' => env('SONG_AUTO_LINK_THRESHOLD', 1.0),
        'candidate_threshold' => env('SONG_CANDIDATE_THRESHOLD', 0.5),
        'candidate_limit' => env('SONG_CANDIDATE_LIMIT', 5),

        /*
        | 新規登録時に「既に登録済みの楽曲か」を判定する際の類似度の閾値です。
        |
        | この判定は検索ではなく同一性の判定なので、あいまい検索より厳しくする。
        | TimestampSongMapping::fuzzySearch() の既定値 0.7 は距離をバイト単位で
        | 数えていた頃に調整された値で、文字単位に変えた後は緩すぎる（実測:
        | 「…カウントダウン / ぴのぴ」と「…カウントダウン / あるふぁきゅん」が
        | 0.75 で一致し、別アーティストの別楽曲が「既に登録済み」として返る）。
        */
        'identity_match_threshold' => env('SONG_IDENTITY_MATCH_THRESHOLD', 0.95),

        /*
        | 紐付け済みマッピングを辞書として照合する際の類似度の閾値です。
        | 装飾を除いたキーがこの値以上に似ている場合、同じ楽曲の候補とみなします。
        */
        'dictionary_similarity_threshold' => env('SONG_DICTIONARY_SIMILARITY_THRESHOLD', 0.8),

        /*
        | 照合キー一覧（楽曲マスタ・辞書）のキャッシュ保持秒数です。
        | 楽曲マスタやマッピングの保存・削除時には自動的に無効化されるため、
        | この値は保険としての上限です。
        */
        'index_cache_ttl' => env('SONG_MATCHING_INDEX_CACHE_TTL', 600),

        /*
        | 候補の絞り込みに n-gram 全文検索インデックスを使用するかの設定です。
        |
        | 現在の照合は楽曲マスタの照合キーを全件メモリに載せた総当たりで行っています。
        | バッチ処理では十分な速度ですが、画面から対話的に候補を出す経路では
        | リクエストごとに全件を読み込むため、規模が大きくなると絞り込みが必要になります。
        |
        | インデックス（songs_match_key_title_fulltext）は作成済みですが、
        | 検索経路はまだ切り替えていないため、この設定は現時点では参照されません。
        | 実データでの効果を確認したうえで切り替えます。
        | なお辞書側（timestamp_song_mappings）を絞り込む場合は、
        | 同テーブルにも照合キーのカラムを持たせる必要があります。
        */
        'use_fulltext_index' => env('SONG_USE_FULLTEXT_INDEX', false),
    ],
];
